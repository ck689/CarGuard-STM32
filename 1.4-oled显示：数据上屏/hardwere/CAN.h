#ifndef _CAN_H
#define _CAN_H

/**
 * @file   CAN.h
 * @brief  CAN总线通信驱动头文件
 * @note   V6 阶段实现，V2 预留。PA11=RX, PA12=TX，需外接CAN收发器
 */

/* V6 待声明：
 * void CAN1_Init(void);
 * void CAN1_SendMsg(uint32_t id, uint8_t *data, uint8_t len);
 * uint8_t CAN1_GetMsg(uint32_t *id, uint8_t *data);
 */

#endif
