/**
 * @file   DMA.c
 * @brief  DMA（直接存储器访问）驱动
 * @note   V5 阶段实现：ADC+DMA多通道连续采集、串口DMA收发
 *         V2 阶段预留文件，暂未实现具体函数
 */
#include "stm32f10x.h"

/* V5 待实现：
 * void DMA_ADC_Init(uint16_t *buf, uint8_t len);  // ADC+DMA多通道采集初始化
 * uint16_t DMA_GetValue(uint8_t channel);           // 从DMA缓冲区读取指定通道值
 */
