#ifndef _DMA_H
#define _DMA_H

/**
 * @file   DMA.h
 * @brief  DMA驱动头文件
 * @note   V5 阶段实现，V2 预留
 */

/* V5 待声明：
 * void DMA_ADC_Init(uint16_t *buf, uint8_t len);
 * uint16_t DMA_GetValue(uint8_t channel);
 */

#endif
